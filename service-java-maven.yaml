apiVersion: v1
kind: Service
metadata:
  name: java-maven-app-svc
  namespace: webapp
  labels:
    app: java-maven-app
spec:
  type: LoadBalancer
  selector:
    app: java-maven-app
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080
